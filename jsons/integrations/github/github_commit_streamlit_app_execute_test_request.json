{
  "module": "plugins.integrations.github_repo_sync_plugin",
  "class": "GitHubRepoSyncPlugin",
  "method": "commit_streamlit_app",
  "constructor_args": {
    "repo_owner": "paglipay",
    "repo_name": "dynamic-exec-service-streamlit-app",
    "branch": "main",
    "allowed_path_prefix": "deploy/heroku"
  },
  "args": [
    {
      "app_content": "import streamlit as st\n\nst.set_page_config(page_title='Dynamic Streamlit UI')\nst.title('Dynamic Streamlit UI')\nst.write('Execute test commit from dynamic-exec-service.')\nst.caption('If you see this caption in production, /execute commit flow worked.')\n\nwith st.form(key='dynamic_form'):\n    name = st.text_input('Name')\n    email = st.text_input('Email')\n    submitted = st.form_submit_button('Submit')\n\nif submitted:\n    st.success('Submitted')\n    st.json({'name': name, 'email': email})\n",
      "file_path": "deploy/heroku/streamlit_app.py",
      "commit_message": "Test /execute commit for Streamlit deploy pipeline",
      "include_heroku_files": true
    }
  ]
}
