# Tests

This project uses `pytest`.

## Run tests

```bash
pytest
```

## Layout

- `tests/unit/` for fast plugin/unit tests
- `tests/api/` for Flask endpoint contract tests
- `tests/conftest.py` for shared fixtures
